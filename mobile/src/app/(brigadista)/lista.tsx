import { Pending } from "@/ui/Pending";

export default function Lista() {
  return (
    <Pending
      screen="2.6"
      title="Headcount · pase de lista"
      task="T-2.11"
      note="Roster × check-ins en vivo (<2 s por WS); marcación delegada distinguible; notificación a no reportados por push OPS."
    />
  );
}
