import { Pending } from "@/ui/Pending";

export default function Panel() {
  return (
    <Pending
      screen="2.1"
      title="Dashboard táctico"
      task="T-2.08"
      note="Salud device_health real (UPS desconocido ⇒ S/D, jamás 0%) + features de 1 s por WS con allowlist topic×rol. Nada de forma de onda (regla de oro 9)."
    />
  );
}
