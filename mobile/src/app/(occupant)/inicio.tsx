import { Pending } from "@/ui/Pending";

export default function Inicio() {
  return (
    <Pending
      screen="1.1"
      title="Modo reposo"
      task="T-2.07"
      note="Estado del edificio por mobile-state (jamás calculado local), próximo/último simulacro, variante SIMULACRO y accesos a rutas/directorio."
    />
  );
}
