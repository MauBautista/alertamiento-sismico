import { Pending } from "@/ui/Pending";

export default function Cuenta() {
  return (
    <Pending
      screen="1.8"
      title="Cuenta"
      task="T-2.07"
      note="Perfil, permisos de alerta, aviso de privacidad, consentimiento GPS revocable y verificación en dos pasos OPCIONAL del ocupante (decisión #7)."
    />
  );
}
