import { Pending } from "@/ui/Pending";

export default function Directorio() {
  return (
    <Pending
      screen="1.7"
      title="Directorio"
      task="T-2.07"
      note="Brigadistas por zona con llamada de un toque (requiere user_profiles.phone — migración 0018, T-2.03)."
    />
  );
}
