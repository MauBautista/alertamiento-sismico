---
// Plantilla de la tarjeta OG (1200×630). No es una página de navegación:
// noindex, fuera del sitemap. scripts/make-og.mjs la captura con Playwright
// y el PNG resultante se commitea como public/og-v1.png.
import "../styles/tokens.css";
---

<!doctype html>
<html lang="es-MX">
  <head>
    <meta charset="utf-8" />
    <title>OG · TAKAB Ailert</title>
    <meta name="robots" content="noindex" />
  </head>
  <body>
    <div class="tarjeta">
      <p class="mono meta">
        <span>takabailert.com</span>
        <span class="sep"></span>
        <span>México</span>
      </p>
      <div class="marcador"></div>
      <p class="titulo">El edificio<br />se protege solo.</p>
      <p class="mono pie">
        TAKAB AILERT · Alertamiento sísmico · Continuidad operativa
      </p>
    </div>
  </body>
</html>

<style>
  body {
    margin: 0;
    background: var(--papel);
    color: var(--tinta);
  }
  .tarjeta {
    box-sizing: border-box;
    width: 1200px;
    height: 630px;
    padding: 56px 64px;
    display: flex;
    flex-direction: column;
    border: 6px solid var(--tinta);
  }
  .mono {
    font-family: var(--fuente-mono);
    font-size: 22px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    margin: 0;
  }
  .meta {
    display: flex;
    align-items: center;
    gap: 24px;
    color: var(--humo);
  }
  .sep {
    flex: 1;
    border-top: 2px solid var(--regla);
  }
  .marcador {
    width: 96px;
    height: 22px;
    background: var(--alerta);
    margin-top: 48px;
  }
  .titulo {
    font-family: var(--fuente-display);
    font-weight: 700;
    font-size: 150px;
    line-height: 0.92;
    text-transform: uppercase;
    letter-spacing: -0.01em;
    margin: 24px 0 0;
  }
  .pie {
    margin-top: auto;
    color: var(--tinta);
  }
</style>
